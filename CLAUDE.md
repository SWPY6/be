# CLAUDE.md

LLM이 흔히 저지르는 코딩 실수를 줄이기 위한 행동 지침. 이 저장소에 적용되는 규칙은 이 파일을 기준으로 한다.

트레이드오프: 이 가이드라인은 속도보다 신중함에 무게를 둔다. 사소한 작업에는 판단에 맡긴다.

## 1. 코딩 전에 생각하기

추측하지 말 것. 혼란을 감추지 말 것. 트레이드오프를 드러낼 것.

구현 전에:

- 전제(assumption)를 명시적으로 말한다. 확신이 없으면 질문한다.
- 해석이 여러 갈래면 모두 제시한다 — 말없이 하나를 고르지 않는다.
- 더 단순한 방법이 있으면 말한다. 필요하면 반대 의견을 낸다.
- 불명확한 게 있으면 멈춘다. 무엇이 혼란스러운지 짚고, 질문한다.

## 2. 단순함이 우선

문제를 푸는 최소한의 코드만. 추측성 코드는 금지.

- 요청하지 않은 기능은 만들지 않는다.
- 한 번만 쓰이는 코드에 추상화를 넣지 않는다.
- 요청하지 않은 "유연성"이나 "설정 가능성"을 넣지 않는다.
- 일어날 수 없는 시나리오에 대한 예외 처리를 넣지 않는다.
- 200줄을 썼는데 50줄로 가능하다면, 다시 쓴다.

스스로에게 묻는다: "시니어 엔지니어가 이걸 보고 과하게 복잡하다고 할까?" 그렇다면 단순화한다.

## 3. 최소 범위의 수정

꼭 건드려야 할 것만 건드린다. 내가 만든 흔적만 치운다.

기존 코드를 수정할 때:

- 인접한 코드·주석·포매팅을 "개선"하지 않는다.
- 망가지지 않은 것을 리팩토링하지 않는다.
- 내 취향과 다르더라도 기존 스타일에 맞춘다.
- 관련 없는 죽은 코드를 발견하면 언급만 한다 — 지우지 않는다.

내 변경이 고아 코드를 만들었을 때:

- 내 변경 때문에 쓰이지 않게 된 import·변수·함수는 제거한다.
- 원래부터 있던 죽은 코드는 요청받지 않는 한 제거하지 않는다.

판별 기준: 변경된 모든 줄이 사용자의 요청으로 곧바로 추적되어야 한다.

## 4. 목표 기반 실행

성공 기준을 정의한다. 검증될 때까지 반복한다.

작업을 검증 가능한 목표로 바꾼다:

- "검증 로직 추가" → "잘못된 입력에 대한 테스트를 작성하고, 통과시킨다"
- "버그 수정" → "버그를 재현하는 테스트를 작성하고, 통과시킨다"
- "X 리팩토링" → "리팩토링 전후로 테스트가 통과하는지 확인한다"

여러 단계로 나뉘는 작업은 간단한 계획을 먼저 말한다:

1. [단계] → 검증: [확인 방법]
2. [단계] → 검증: [확인 방법]
3. [단계] → 검증: [확인 방법]

성공 기준이 명확하면 스스로 반복하며 진행할 수 있다. 기준이 약하면("동작하게 만들어줘") 계속 확인을 요구하게 된다.

---

이 가이드라인이 작동하고 있다는 신호: diff에 불필요한 변경이 줄어들고, 과도한 복잡성 때문에 다시 작성하는 일이 줄고,
실수한 뒤가 아니라 구현 전에 명확화 질문이 나온다.

---

# 프로젝트: ploutos

## 스택

- Java 26 (Gradle toolchain), Spring Boot 4.1.1
- 단일 Gradle 모듈. `settings.gradle`의 `rootProject.name = 'ploutos'`
- 베이스 패키지: `org.example.ploutos`
- 의존성: `spring-boot-starter-security`, `spring-boot-starter-webmvc`, `mysql-connector-j`(runtime)
- 테스트: JUnit 6 (`useJUnitPlatform`), `spring-boot-starter-webmvc-test`, `spring-boot-starter-security-test`

## 적용 범위

- 이 저장소의 규칙은 이 파일을 기준으로 한다. 다른 저장소를 전제로 한 규칙(다중 모듈 배치, 언어 전환 정책, 별도 쿼리 DSL·마이그레이션 도구 등)은 여기에 적용하지 않는다.
- 이 프로젝트는 **Java**로 작성한다. 신규 코드도 Java로 쓴다.
- 아직 도입되지 않은 기술(JPA, 쿼리 DSL, 마이그레이션 도구, Redis, 메시지 브로커 등)을 전제한 코드를 임의로 추가하지 않는다. 필요하면 먼저 제안하고 `build.gradle`에 의존성을 추가한 뒤 사용한다.

## 코드 스타일

- 객체지향과 SOLID를 준수한다. 의존성은 생성자를 통해 외부에서 주입한다.
- 인터페이스는 필요한 부분만 노출한다.
- **`@Getter`/`@Setter` 금지. `@Data` 금지.** 접근자를 일괄 생성하지 않는다. 정말 필요한 값만 의도가 드러나는 메서드로 노출하고, 가능하면 값을 꺼내 판단하지 말고 객체가 판단하게 한다.
- Setter를 쓰지 않는다. 객체는 생성 시점에 완전한 상태여야 하며, 상태 변경은 의도가 드러나는 도메인 메서드로 한다.
- 디미터 법칙을 지킨다. Low Coupling / High Cohesion.
- **`else` 금지.** early return / guard clause를 적극 사용해 중첩을 없앤다.
- DTO는 `sealed interface` + `record` 조합으로 작성한다.
- 엔티티는 작게 유지하고, `@Column`으로 테이블 정보를 명시한다.
- 파일 끝에 개행(EOF)을 넣는다.

## 개발 방식: SDD (Spec Driven Development)

코드를 쓰기 전에 명세를 먼저 쓴다. 명세 없이 구현을 시작하지 않는다.

순서는 **명세 → 구현 → 테스트**다.

1. 무엇을 만들지 명세로 합의한다 — 입력, 출력, 규칙, 예외 상황.
2. 명세의 인수 기준을 만족하도록 구현한다.
3. 그 인수 기준이 실제로 만족되는지 BDD 테스트로 증명한다.

명세가 이끌고 테스트가 증명한다. "실패하는 테스트를 먼저 쓰고 통과시킨다"는 TDD 사이클은 쓰지 않는다.
**단, 버그만은 예외다** — 고치기 전에 재현 테스트를 먼저 쓰고, 그것이 실패하는 것을 확인한 뒤 수정한다.

- 요구사항이 막연하면 구현하지 말고 명세부터 확정한다 (행동 가이드라인 1번과 같은 원칙).
- 명세가 바뀌면 명세를 먼저 고치고 구현과 테스트가 따라간다.
- 명세는 `docs/SPEC-<모듈id>.md`에 저장한다. 계획과 태스크 목록은 `tasks/plan.md`, `tasks/todo.md`에 둔다.

## 테스트

- **JUnit 6** (`org.junit.jupiter`, `useJUnitPlatform()`).
- 모든 테스트는 **BDD**로 작성한다. `// given` `// when` `// then` 주석으로 구간을 명시한다.
- 테스트 메서드명은 **한글로 `조건_결과`** 형식으로 쓴다.
  - `잔고가_있으면_결제가_성공한다`
  - `잔고가_부족하면_결제가_실패한다`
- 테스트 클래스는 `*Test`.
- 단위 테스트 커버리지를 높이는 것을 우선한다. 새 로직은 테스트 없이 머지하지 않는다.

### 테스트 피라미드

| 종류 | 비중 | 대상 |
| --- | --- | --- |
| 단위 테스트 | **60%** | 도메인 객체·서비스 로직. 스프링 컨텍스트 없이 POJO로 검증 |
| 통합 테스트 | **30%** | 여러 계층·DB 연동. 슬라이스 테스트, `@SpringBootTest` |
| E2E 테스트 | **10%** | HTTP 요청부터 응답까지 전체 흐름 |

비중이 역전되면(E2E가 많고 단위가 적으면) 느리고 잘 깨지는 테스트 묶음이 된다. 단위 테스트로 내릴 수 있는 검증은 내린다.

```java
@Test
void 잔고가_있으면_결제가_성공한다() {
    // given
    Account account = new Account(10_000);

    // when
    PaymentResult result = account.pay(3_000);

    // then
    assertThat(result.isSuccess()).isTrue();
}
```

## 빌드 & 실행

```bash
./gradlew build          # 빌드
./gradlew test           # 테스트
./gradlew bootRun        # 로컬 실행
```
